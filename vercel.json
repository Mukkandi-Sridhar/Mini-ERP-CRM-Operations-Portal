{
  "version": 2,
  "buildCommand": "pnpm build:types && pnpm --filter api run prisma:generate && pnpm build:api && pnpm build:web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
